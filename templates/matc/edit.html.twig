{% extends 'base.html.twig' %}

{% block title %}Edit Matc{% endblock %}

    

{% block body %}

    <h1>Modifier Match</h1>

    <div style="text-align: center;">
        <div style="text-align: center; margin-right: 100px;">
            {{ include('matc/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>

        <div style="display: inline-block; margin-right: 10px;">
            <a href="{{ path('app_matc_index') }}" class="btn1 btn1-save">Retour à la liste</a>
        </div>

        <div style="display: inline-block;margin-right: 10px;">
            {{ include('matc/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}


