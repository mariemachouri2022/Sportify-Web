<form method="post" action="{{ path('app_arbitre_delete', {'idArbitre': arbitre.idArbitre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arbitre.idArbitre) }}">
    <button class="btn">Delete</button>
</form>
