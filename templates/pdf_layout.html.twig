<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ equipe.nom }} PDF</title>
    <style>
        
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            color: #333;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        h1 {
            color: #007bff;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{ equipe.nom }}</h1>

        <table>
            <tr>
                <th>Id</th>
                <td>{{ equipe.IDEquipe }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ equipe.nom }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ equipe.niveau }}</td>
            </tr>
            <tr>
                <th>Random</th>
                <td>{{ equipe.israndom }}</td>
            </tr>
             <tr>
                <th>Rank</th>
                <td>{{ equipe.rank }}</td>
            </tr>
           <td>
                    {% for player in equipe.utilisateurs %}
                        {{ player.nom }} {% if not loop.last %}, {% endif %}
                    {% endfor %}
                </td>
        </table>
    </div>
</body>
</html>