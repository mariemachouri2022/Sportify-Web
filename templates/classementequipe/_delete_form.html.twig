<form method="post" action="{{ path('app_classementequipe_delete', {'id': classementequipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classementequipe.id) }}">
    <button class="btn">Delete</button>
</form>
