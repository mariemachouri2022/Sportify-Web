<form method="post" action="{{ path('app_equipe_delete', {'IDEquipe': equipe.IDEquipe}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipe.IDEquipe) }}">
    <button class="btn">Delete</button>
</form>
